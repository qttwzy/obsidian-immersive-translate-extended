name: Quality

on:
  pull_request:
  push:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Check out source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Set up Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 22
          cache: npm

      - name: Install development dependencies
        run: npm ci --ignore-scripts

      - name: Run product checks
        run: npm run quality

      - name: Assemble the plugin ZIP
        run: npm run package:build -- --root "$PWD" --out "$RUNNER_TEMP/plugin-package"

      - name: Confirm one package asset
        run: test "$(find "$RUNNER_TEMP/plugin-package" -maxdepth 1 -type f -name '*.zip' | wc -l | tr -d ' ')" = "1"
